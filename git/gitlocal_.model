#!/usr/bin/env bash

if [[ -z "$VAR_USER_NAME" ]]; then
    echo "ERROR: User name variable is not set" >&2
    false; exit
fi

if [[ -z "$VAR_USER_EMAIL" ]]; then
    echo "ERROR: User email variable is not set" >&2
    false; exit
fi

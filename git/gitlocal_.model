#!/usr/bin/env bash

if [[ -z "$VAR_USER_NAME" ]]; then
    echo "ERROR: user name variable is not set" >&2
    false; exit
fi

if [[ -z "$VAR_EMAIL_ADDRESS" ]]; then
    echo "ERROR: email address variable is not set" >&2
    false; exit
fi
